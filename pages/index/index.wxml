<view class="pages">
  <block wx:for="{{ pages }}" wx:key="name">
    <view>
      <button type="primary" bindtap="onBtnClick" data-item="{{item}}">{{ item.name }}</button>
    </view>
  </block>
</view>